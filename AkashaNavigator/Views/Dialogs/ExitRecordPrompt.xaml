<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Dialogs.ExitRecordPrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        xmlns:vm="clr-namespace:AkashaNavigator.ViewModels.Dialogs"
        Title="记录提示"
        Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Foreground="White"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False">

    <!-- 主容器 -->
    <Border x:Name="MainContainer" Background="#2D2D2D" CornerRadius="8" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 标题栏 -->
                <RowDefinition Height="*"/>     <!-- 内容区 -->
                <RowDefinition Height="Auto"/>  <!-- 按钮区 -->
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 图标 -->
                    <TextBlock Grid.Column="0" Text="💾" FontSize="16"
                               VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <!-- 标题 -->
                    <TextBlock Grid.Column="1" Text="记录提示"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="White" VerticalAlignment="Center"/>

                    <!-- 关闭按钮 -->
                    <Button Grid.Column="2" Style="{StaticResource DialogCloseButtonStyle}"
                            Command="{Binding CloseCommand}" ToolTip="取消"/>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Border Grid.Row="1" Padding="20,16">
                <StackPanel>
                    <!-- 提示文本 -->
                    <TextBlock Text="当前页面尚未记录，是否需要记录到开荒笔记？"
                               FontSize="14" Foreground="#DDD"
                               TextWrapping="Wrap" LineHeight="22"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16"/>

                    <!-- 页面预览区域 -->
                    <Border Background="#252525" CornerRadius="6" Padding="12,10" Margin="0,0,0,8">
                        <StackPanel>
                            <!-- 页面标题 -->
                            <TextBlock Text="{Binding PageTitle}"
                                       FontSize="13" Foreground="White"
                                       TextTrimming="CharacterEllipsis"
                                       TextWrapping="NoWrap"
                                       Margin="0,0,0,4"/>

                            <!-- 页面 URL -->
                            <TextBlock Text="{Binding PageUrl}"
                                       FontSize="11" Foreground="#888"
                                       TextTrimming="CharacterEllipsis"
                                       TextWrapping="NoWrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 按钮区 -->
            <Border Grid.Row="2" Background="#252525" CornerRadius="0,0,8,8" Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- 打开开荒笔记按钮 -->
                    <Button Content="📂 打开开荒笔记"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding OpenPioneerNotesCommand}"
                            Margin="0,0,8,0"/>

                    <!-- 快速记录按钮 -->
                    <Button Content="💾 快速记录"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding QuickRecordCommand}"
                            Margin="0,0,8,0"/>

                    <!-- 直接退出按钮 -->
                    <Button Content="直接退出"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding DirectExitCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
